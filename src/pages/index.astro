---
import BaseLayout from "../layouts/BaseLayout.astro";
import Hero from "../components/Hero.astro";
import WorkflowSteps from "../components/WorkflowSteps.astro";
import FeatureCard from "../components/FeatureCard.astro";
import SectionHeading from "../components/SectionHeading.astro";
import ScreenshotFrame from "../components/ScreenshotFrame.astro";
import PrivacyPathCard from "../components/PrivacyPathCard.astro";
import FAQItem from "../components/FAQItem.astro";
import PrimaryButton from "../components/PrimaryButton.astro";
import SecondaryButton from "../components/SecondaryButton.astro";
import PlatformBadge from "../components/PlatformBadge.astro";
import { downloadManifest } from "../data/downloadManifest";
import { downloadStats, formatDownloadCount } from "../data/downloadStats";
import { sitePath } from "../lib/urls";

const macos = downloadManifest.platforms["macos-arm64"];
const windows = downloadManifest.platforms["windows-x64"];
const totalDownloads = downloadStats.totals.appPackageDownloads + downloadStats.totals.modelDownloads;
---

<BaseLayout footerDownloadTotal={formatDownloadCount(totalDownloads)}>
  <Hero macos={macos} windows={windows} />

  <section class="section-compact" aria-labelledby="workflow-title">
    <div class="shell">
      <SectionHeading eyebrow="怎么用" title="按下快捷键，说完就行" description="录音、识别、整理和写入都会显示进度。中途出了问题，也能看到文字有没有保住。" />
      <h2 id="workflow-title" class="visually-hidden">语音输入的四个步骤</h2>
      <WorkflowSteps />
    </div>
  </section>

  <section class="section section-tinted" aria-labelledby="values-title">
    <div class="shell">
      <SectionHeading eyebrow="用起来" title="不用先打开另一个窗口" description="写邮件也好，回消息也好，光标停在哪里，就在那里开始说。" />
      <h2 id="values-title" class="visually-hidden">三项核心价值</h2>
      <div class="grid-3">
        <FeatureCard index="01" title="光标在哪，就写到哪">按下快捷键，在常用的办公、沟通和写作软件里直接说。个别应用可能会有兼容差异。</FeatureCard>
        <FeatureCard index="02" title="还是你原来的意思">它会去掉明显的重复和口头语，顺一顺标点，但不该替你改事实、数字和观点。</FeatureCard>
        <FeatureCard index="03" title="想用本地识别，也可以">本地模型可以单独安装。语音在哪儿识别、文字会不会交给云端整理，设置里都能看到。</FeatureCard>
      </div>
    </div>
  </section>

  <section class="section" aria-labelledby="file-transcription-title">
    <div class="shell">
      <SectionHeading eyebrow="已有录音" title="手机录音和会议录音，也可以直接整理" description="把音频文件拖进来，纤云叙会在后台生成逐字稿。多人录音还可以区分不同发言人，再由你校正姓名和片段归属。" />
      <h2 id="file-transcription-title" class="visually-hidden">文件转写与多人说话人分离</h2>
      <div class="grid-3">
        <FeatureCard index="01" title="常见格式直接导入">支持 WAV、MP3、M4A 和 FLAC。处理在后台继续，不会卡住日常语音输入。</FeatureCard>
        <FeatureCard index="02" title="逐字稿带时间信息">每段文字保留开始和结束时间，方便回听、复制，并导出 TXT 或 Markdown。</FeatureCard>
        <FeatureCard index="03" title="多人录音可以分开看">可自动估计或指定说话人数。系统只使用“说话人 1、2、3”等标签，不会猜测真实身份。</FeatureCard>
      </div>
    </div>
  </section>

  <section class="section section-tinted" aria-labelledby="screenshots-title">
    <div class="shell">
      <SectionHeading eyebrow="现在的样子" title="打开以后，你会看到这些" description="下面都是实际运行截图。示例文字经过处理，不含密钥、私人路径或真实办公内容。" />
      <h2 id="screenshots-title" class="visually-hidden">产品截图</h2>
      <div class="screenshot-featured">
        <ScreenshotFrame
          title="运行准备与系统权限"
          description="快捷键、麦克风、识别方式、文字整理和系统权限，都放在首页。"
          imageBase="home-dashboard-macos"
          alt="纤云叙 macOS 首页，显示运行准备、快捷键、本地识别和系统权限状态"
          aspect="window"
        />
      </div>
      <div class="grid-2">
        <ScreenshotFrame
          title="文件转写与多人分离"
          description="导入录音后，可以看到时长、识别模型、处理进度和说话人数，并复制或导出结果。"
          imageBase="file-transcription-speakers-macos"
          alt="纤云叙文件转写页面，显示四人测试录音、本地识别、说话人分离和导出操作"
        />
        <ScreenshotFrame
          title="说话人时间轴"
          description="按时间查看各位说话人的片段；多人重叠和不确定结果会单独标出，方便人工确认。"
          imageBase="file-transcription-speakers-sanitized-macos"
          alt="纤云叙多人录音时间轴，显示说话人片段和多人重叠提示"
          width={560}
          height={700}
        />
      </div>
      <div class="grid-2">
        <ScreenshotFrame
          title="个人词典"
          description="把姓名、机构名和专业词记下来；常见错误写法保留在词条详情中。"
          imageBase="personal-dictionary-macos"
          alt="纤云叙个人词典页面，显示正确词条、自动添加和手工添加来源"
        />
        <ScreenshotFrame
          title="本地模型管理"
          description="SenseVoiceSmall 的下载、校验和加载状态都在设置里；多人分离扩展包也从同一处安装。"
          imageBase="local-model-macos"
          alt="纤云叙设置页中的 SenseVoiceSmall 本地模型状态和隐私说明"
        />
      </div>
      <div class="screenshot-overlay-feature">
        <ScreenshotFrame
          title="录音状态浮层"
          description="录音时，屏幕底部会出现一个轻量波形。浮层不会抢走光标，也不会挡住鼠标操作。"
          imageBase="status-overlay-recording-macos"
          alt="纤云叙在浅色应用背景上显示录音中的圆形状态浮层"
          width={640}
          height={300}
        />
      </div>
    </div>
  </section>

  <section class="section section-tinted" aria-labelledby="paths-title">
    <div class="shell">
      <SectionHeading eyebrow="数据去向" title="语音在哪识别，文字会不会联网" description="用本地识别时，语音留在电脑上。如果又打开了云端文字整理，识别后的文字还是会发给你选的服务。" />
      <h2 id="paths-title" class="visually-hidden">本地与云端的数据去向</h2>
      <div class="grid-3">
        <PrivacyPathCard title="本地识别 · 不用 AI 整理" path="语音 → 本机识别 → 基础整理 → 写入" tone="local">识别时不需要把语音发到云端。本地模型由你主动下载安装。</PrivacyPathCard>
        <PrivacyPathCard title="本地识别 · 云端 AI 整理" path="本机识别语音 → 云端整理文字 → 写入" tone="mixed">音频在本机识别，识别后的文字会按你的设置交给云端服务整理。</PrivacyPathCard>
        <PrivacyPathCard title="云端识别 · 可选 AI 整理" path="云端识别语音 → 可选整理 → 写入" tone="cloud">音频会发送给你选择的识别服务，数据如何保留由对应服务的政策决定。</PrivacyPathCard>
      </div>
      <div style="margin-top: var(--space-5)"><SecondaryButton href={sitePath("/privacy/")}>查看完整隐私说明</SecondaryButton></div>
    </div>
  </section>

  <section class="section" aria-labelledby="platforms-title">
    <div class="shell grid-2">
      <div>
        <SectionHeading eyebrow="支持平台" title="Mac 和 Windows 都有安装包" description="macOS Apple Silicon 版已完成当前版本真机验证。Windows x64 版由 GitHub Actions 构建发布，新增功能还在等待更多 Windows 真机反馈。" />
        <h2 id="platforms-title" class="visually-hidden">平台支持状态</h2>
        <div class="hero-badges">
          <PlatformBadge tone="success">macOS Apple Silicon · Beta</PlatformBadge>
          <PlatformBadge>Intel Mac · 暂不支持</PlatformBadge>
          <PlatformBadge tone="success">Windows x64 · Beta</PlatformBadge>
        </div>
      </div>
      <div class="cta-band">
        <div><h2>你用哪台电脑？</h2><p>Mac 和 Windows 都准备了单独的安装包与安装步骤。</p></div>
        <div class="button-row"><PrimaryButton href={sitePath("/download/")}>立即下载</PrimaryButton><SecondaryButton href={sitePath("/install/")}>安装说明</SecondaryButton></div>
      </div>
    </div>
  </section>

  <section class="section section-tinted" aria-labelledby="privacy-title">
    <div class="shell grid-2">
      <div>
        <SectionHeading eyebrow="你可以自己定" title="历史要不要留，云端整理要不要开" description="历史可以只留统计，也可以完全关闭。本地模型只有在你点击安装后才会下载。" />
        <h2 id="privacy-title" class="visually-hidden">隐私与透明性</h2>
      </div>
      <div class="prose">
        <p><strong>刚装好时：</strong>只保存使用统计，保留 30 天，并自动删除临时音频。具体设置可以随时在应用里改。</p>
        <p><strong>如果出错：</strong>文字整理失败会退回基础文本；写不进输入框时，会尝试复制到剪贴板。</p>
        <p><a href={sitePath("/privacy/")}>看看数据会去哪里</a></p>
      </div>
    </div>
  </section>

  <section class="section" aria-labelledby="download-cta-title">
    <div class="shell cta-band">
      <div><h2 id="download-cta-title">选好系统，就可以下载</h2><p>Mac 和 Windows 都有单独的安装包，第一次安装也准备了详细步骤。</p></div>
      <div class="button-row">
        <PrimaryButton href={sitePath("/download/")}>前往下载中心</PrimaryButton>
        <SecondaryButton href={sitePath("/install/")}>安装说明</SecondaryButton>
      </div>
    </div>
  </section>

  <section class="section section-tinted" aria-labelledby="faq-title">
    <div class="shell">
      <SectionHeading eyebrow="常见问题" title="几个大家经常会问的问题" />
      <h2 id="faq-title" class="visually-hidden">常见问题摘要</h2>
      <div class="faq-list">
        <FAQItem question="Mac 和 Windows 都可以用吗？" open>可以下载 macOS Apple Silicon Beta 和 Windows x64 Beta。macOS 当前版本已经过真机验证；Windows 安装包已发布，但文件转写和多人分离仍在等待更多 Windows 真机反馈。Intel Mac 暂不支持。</FAQItem>
        <FAQItem question="多人录音能自动认出姓名吗？">不能。纤云叙只把不同声音标成“说话人 1、2、3”等，你可以在当前任务里手工改名。它不会建立跨录音的永久声纹身份。</FAQItem>
        <FAQItem question="用了本地识别，就完全不会联网吗？">不一定。本地识别只负责把语音变成文字；如果你开启云端 AI 整理，识别后的文字仍可能联网。</FAQItem>
        <FAQItem question="为什么需要麦克风和辅助功能权限？">麦克风用于录音，辅助功能用于把整理好的文字写进你正在使用的应用。两项权限都可以在系统设置中管理。</FAQItem>
        <FAQItem question="输入失败后，文字还能找回来吗？">AI 整理失败时会尽量保留基础文本；写入失败时会尝试复制到剪贴板。最终结果请以应用当时的提示为准。</FAQItem>
      </div>
      <div style="margin-top: var(--space-5)"><SecondaryButton href={sitePath("/faq/")}>查看全部问题</SecondaryButton></div>
    </div>
  </section>

</BaseLayout>
