---
import BaseLayout from "../../layouts/BaseLayout.astro";
import PrimaryButton from "../../components/PrimaryButton.astro";
import SecondaryButton from "../../components/SecondaryButton.astro";
import StatusNotice from "../../components/StatusNotice.astro";
import FAQItem from "../../components/FAQItem.astro";
import { downloadManifest } from "../../data/downloadManifest";
import { sitePath } from "../../lib/urls";

const windows = downloadManifest.platforms["windows-x64"];
---

<BaseLayout title="Windows 安装说明" description="安装纤云叙 Windows x64 Beta：完成下载、文件校验、麦克风设置和第一次输入。">
  <header class="page-hero"><div class="shell"><p class="eyebrow">Windows 安装说明</p><h1>在 Windows 上安装，并完成第一次输入。</h1><p>下载 Windows x64 安装程序，核对文件后，跟着页面完成安装、麦克风设置和第一次输入。</p></div></header>
  <section class="section-compact"><div class="shell"><StatusNotice title="按照系统步骤确认即可">不需要关闭 Windows 安全中心或防病毒软件。遇到系统提示时，请继续查看本页对应步骤。</StatusNotice></div></section>
  <section class="section"><div class="shell grid-2">
    <ol class="install-steps">
      <li><h2>下载安装程序</h2><p>从本页下载 <code>{windows.fileName}</code>，确认来源域名是 GitHub Releases。</p></li>
      <li><h2>核对 SHA-256</h2><p>从下载页复制校验值，再用 Windows 文件校验工具或 PowerShell 的 <code>Get-FileHash</code> 核对。两边不一致时，请不要运行安装程序。</p></li>
      <li><h2>打开安装程序</h2><p>双击下载好的文件。如果系统显示运行确认，请先核对文件来源，再继续下一步。</p></li>
      <li><h2>阅读 SmartScreen 提示</h2><p>只有在来源和校验值均正确时，才选择查看详细信息并继续。不要为了安装而永久关闭 SmartScreen。</p></li>
      <li><h2>完成安装</h2><p>按照安装程序的提示继续即可。不要从第三方下载站获取别人重新打包的版本。</p></li>
      <li><h2>首次启动</h2><p>从开始菜单打开纤云叙。如果系统要求 WebView2 运行环境，请只使用 Microsoft 官方来源完成安装。</p></li>
      <li><h2>检查麦克风权限</h2><p>进入“设置 → 隐私和安全性 → 麦克风”，允许桌面应用访问麦克风，然后回到纤云叙刷新状态。</p></li>
      <li><h2>安装本地模型</h2><p>如选择 SenseVoiceSmall，在应用设置中主动下载安装并等待校验完成。模型不会包含在当前 Setup 中。</p></li>
      <li><h2>确认快捷键</h2><p>检查全局快捷键没有与其他应用冲突。管理员权限运行的目标应用可能阻止普通权限应用写入文本。</p></li>
      <li><h2>试一次语音输入</h2><p>打开一个不含敏感信息的记事本文档，按快捷键说一句话，确认录音、识别、整理和写入都能正常完成。</p></li>
    </ol>
    <aside>
      <div class="button-row"><PrimaryButton href={windows.downloadUrl} external>下载 v{downloadManifest.version} Windows Beta</PrimaryButton><SecondaryButton href={sitePath("/download/")}>查看校验值</SecondaryButton></div>
      <div style="margin-top: var(--space-6)"><StatusNotice tone="info" title="目前验证到哪里了？">Windows 10 x64 已完成实机验证。Windows 11 x64、ARM64，以及不同公司的安全策略，还需要进一步确认。</StatusNotice></div>
    </aside>
  </div></section>
  <section class="section section-tinted"><div class="shell">
    <h2 style="margin-bottom: var(--space-5)">常见安装问题</h2>
    <div class="faq-list">
      <FAQItem question="SmartScreen 提示未知发布者怎么办？" open>这是因为当前 Beta 还没有代码签名。请先确认文件来自官方 Release，并核对 SHA-256；无法确认来源时，不要继续运行，也不要全局关闭 SmartScreen。</FAQItem>
      <FAQItem question="麦克风一直不可用怎么办？">检查 Windows 的麦克风隐私设置是否允许桌面应用访问，同时确认默认麦克风可以正常录音。修改后回到纤云叙，再检查一次。</FAQItem>
      <FAQItem question="为什么文字写不进管理员应用？">Windows 会限制普通应用向管理员应用模拟输入。建议先用普通权限打开目标应用；不要为了绕过限制，长期以管理员身份运行纤云叙。</FAQItem>
      <FAQItem question="如何卸载？">退出纤云叙，进入“设置 → 应用 → 已安装的应用”，找到纤云叙并选择卸载。本地历史和模型是否保留，请以卸载程序提示和应用数据目录中的实际内容为准。</FAQItem>
    </div>
  </div></section>
</BaseLayout>
