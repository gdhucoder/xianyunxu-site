---
import BaseLayout from "../layouts/BaseLayout.astro";
import PrivacyPathCard from "../components/PrivacyPathCard.astro";
import SectionHeading from "../components/SectionHeading.astro";
import StatusNotice from "../components/StatusNotice.astro";
---
<BaseLayout title="隐私说明" description="了解纤云叙如何处理语音、文字、历史记录、临时音频、本地模型和诊断信息。">
  <header class="page-hero"><div class="shell"><p class="eyebrow">隐私说明</p><h1>你的语音和文字，会去哪里？</h1><p>下面按当前版本的实际功能说明。暂时还不能确认的地方，会直接标成“需要进一步确认”，不会用模糊承诺带过。</p></div></header>
  <section class="section-compact"><div class="shell"><StatusNotice title="本地识别不等于全程离线">本地语音识别只表示音频在电脑上完成识别。如果开启云端 AI 整理，识别后的文字仍可能发送给你配置的服务。</StatusNotice></div></section>
  <section class="section"><div class="shell">
    <SectionHeading eyebrow="数据去向" title="常见的三种用法" />
    <div class="grid-3">
      <PrivacyPathCard title="本地识别，不用 AI 整理" path="语音 → 本机模型 → 基础文本 → 当前应用" tone="local">识别语音时不需要连接云端。本地模型需要由你主动下载安装。</PrivacyPathCard>
      <PrivacyPathCard title="本地识别，使用云端 AI 整理" path="本机识别语音 → 云端整理文字 → 当前应用" tone="mixed">音频留在电脑上识别，识别后的文字会按你的设置发送给云端服务整理。</PrivacyPathCard>
      <PrivacyPathCard title="云端识别，可选 AI 整理" path="云端识别语音 → 可选整理 → 当前应用" tone="cloud">音频会发送给你选择的识别服务。服务如何处理和保留数据，请查看相应服务商的政策。</PrivacyPathCard>
    </div>
  </div></section>
  <section class="section section-tinted"><div class="shell grid-2">
    <div class="prose">
      <h2>本地历史</h2>
      <p>历史记录保存在这台电脑的本地数据库中，不会把正文放在浏览器存储里。</p>
      <ul><li><strong>不保存（off）：</strong>文本和统计都不保存。</li><li><strong>只存统计（stats_only）：</strong>只保存次数、时长、字数、耗时和任务结果。</li><li><strong>只存最终文本（final_only）：</strong>保存最终文本和统计。</li><li><strong>原文和最终文本（raw_and_final）：</strong>两种文本和统计都会保存。</li></ul>
      <p>当前默认设计是：只存统计、保留 30 天、自动删除临时音频。最终请以应用设置页显示为准。</p>
    </div>
    <div class="prose">
      <h2>临时音频</h2>
      <p>默认不会把音频作为历史记录长期保存，也可以在任务完成后自动删除临时音频。</p>
      <p><strong>需要进一步确认：</strong>断电、程序崩溃或其他异常退出时，临时音频会在什么时候清理完毕。</p>
      <h2>模型文件</h2>
      <p>只有在你点击安装后，应用才会下载本地模型。模型保存在系统的应用数据目录中，也可以从设置里删除。模型文件本身不包含你的语音或文字。</p>
    </div>
  </div></section>
  <section class="section"><div class="shell grid-2">
    <div class="prose"><h2>日志与密钥</h2><p>API Key 通过应用的安全配置读取，不应出现在普通日志、截图或公开配置里。普通日志也不应记录完整的语音转写正文。</p><p><strong>需要进一步确认：</strong>诊断日志具体保留多久、导出时包含什么，以及是否提供一键删除。</p></div>
    <div class="prose"><h2>崩溃与诊断信息</h2><p>这个官网不使用 Cookie 分析、广告或营销追踪，页面显示也不依赖浏览器实时调用 GitHub API。</p><p><strong>需要进一步确认：</strong>桌面应用是否默认收集自动崩溃报告；如果以后启用，如何征得用户同意并允许删除。</p></div>
  </div></section>
  <section class="section section-tinted"><div class="shell"><StatusNotice tone="warning" title="使用云端服务前，请查看服务商条款">云端语音识别和 AI 整理如何传输、处理和保留数据，由你选择的服务和配置决定。本官网不会替第三方承诺“绝不保留”。填写 API Key 前，请先阅读对应服务的条款。</StatusNotice></div></section>
</BaseLayout>
